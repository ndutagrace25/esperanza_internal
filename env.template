# Database
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://user:password@localhost:5432/esperanza_internal?schema=public"

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (generate a strong random string)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Email Configuration (SMTP)
# For Gmail: Use App Password (see SMTP_SETUP.md for instructions)
# For Outlook: Use App Password
# For Yahoo: Use App Password
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password-here"
SMTP_FROM="your-email@gmail.com"

# API Keys (add as needed)
# API_KEY=your_api_key_here

